// Booking destinations. Swap these constants when the real scheduling links
// exist; every call to action on the site reads from here.
export const NIDA_BOOKING_URL = 'mailto:hello@futurevoices.co'
export const PREP_BOOKING_URL = 'mailto:hello@futurevoices.co'

export const CONTACT_EMAIL = 'hello@futurevoices.co'

/**
 * MailerLite form endpoint. Until a real URL is supplied, this stays empty and
 * newsletter signups are stored the same way contact enquiries are, through
 * /api/newsletter, so nothing is lost in the meantime.
 */
export const NEWSLETTER_ENDPOINT = '[NEWSLETTER_ENDPOINT]'

// Square-bracket values are deliberate placeholders. Fill them in before launch.
export const NEXT_COHORT = {
  ages: '[AGE RANGE]',
  starts: '[START DATE]',
}

export const LOCATION = '[MISSISSAUGA OR GTA]'
